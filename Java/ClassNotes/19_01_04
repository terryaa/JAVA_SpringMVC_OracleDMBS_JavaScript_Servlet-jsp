Chapter 6 클래스

Singleton과 final알기

* class에 final을 주면 확장가능하지않은(상속불가)한 클래스가된다.
* default 접근제한자의 영역은 같은 패키지안에서만 public처럼 사용된다.
* 접근제한자알기

멤버필드
* 스태틱이냐아니냐
* 파이널이냐아니냐
* 상수냐아니냐
멤버메소드
* 스태틱이냐
* 추상이냐

Chapter 7 상속

인터페이스와 구현 클래스. 인터페이스에 추가하여 구현한게 구현클래스.
시험때 테스트무조건합니다!

Super, super()  vs this,this()
* ()들은 기본생성자를 호출한다.
* super는 부모클래스의 필드와메소드를 사용할수있게해준다.
* 자식 클래스의 객체를 생성하면 자동으로 super가 실행, 부모객체가 먼저 생성되고 자식객체가생성.
* 자식객체와 부모객체는 상속관계로 연결, 자식객체의 주소공간안에같이들어감.
* Super()는 보통 생략해도 알아서실행됨.

C1, C2 가 P를 상속받는다고하고,
C1 c1 = new C1();
P  c2 = new C2();
메모리 100쯤에, C1,P객체가 생성. 상속관계로묶인다.
메모리 200쯤에, C2, P객체가 생성. 상속관게로 묶인다.
변수 c2는 메모리200을 참조하지만, 접근은 200쯤에 있는 P객체에만 접근가능하다.


다형성
* 여러 객체를 한가지 공통점을 통하여 참조할수있는것.
* P c1=new C1();
* P c2=new C2();
* P 라는 부모 클래스 변수 하나로 상속을받는 다양한 객체 참조 가능.
메소드 override
메소드안에서 지역변수가 this의 변수보다 더 우선순위가있듯이, 자식 클래스에서 오버라이드시 변수가우선순위를 같는다. 즉, 자식클래스에서 override한 메소드를 자식객체에서 호출시, override된 메소드 호출됨. 

* 부모메소드와 동일한 시그니처(리턴타임,메소드이름,매개변수)
* 접근제한 '더 강하게'불가
* 새로운 예외 불가
* override된 메소드는 자식객채를 참조하는 부모클래스 변수가 호출하게되면 부모객체의 메소드를 호출하지만 override하였기때문에 자식객체의 메소드를 불러온다.

어노테이션:실행가능한주석

Chapter 7.8 추상클래스

* 공통적인 특징을 뽑아서 추상적인 클래스를 만든다. <=> 실제생성하는것은 실체클래스
* 추상클래스는 new를통한 객체생성을 할수없다.
* 따라서 추상메소드를 쓰기위해선 자식을 만들어 상속받아 사용한다.
* 미완성메서드. {}가없다. 
* 추상 메소드를 포함한 클래스는 반드시 추상 클래스여야한다.
* 추상 메소드가없는 추상클래스는 가능하다
* 추상클래스를 상속하는 클래스는 추상 메서드에대해서 강제로 override를하게만든다. 구현하지않으면 진행안됨

 이 방식으로  굳이 새로운 클래스를 만들지 않더라도 추상 클래스의 메서드를 오버라이드하여 사용할수있다.

시험 : 자바 IO
응용소프트웨어시험 : 네트워킹
